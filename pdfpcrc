bind Next      next
bind Return    next
bind Right     next
bind space     next
bind Down      nextOverlay
bind S+Next    next10
bind S+Return  next10
bind S+Right   next10
bind S+space   next10
bind S+Down    next10

bind Left      prev
bind Prior     prev
bind Up        prevOverlay
bind S+Left    prev10
bind S+Prior   prev10
bind S+Up      prev10

bind Home      gotoFirst
bind End       gotoLast
bind g         goto

bind Escape    quit
bind q         quit

bind s         start
bind p         pause
bind r         resetTimer
bind R         reset

bind b         blank
bind period    blank
bind f         freeze

bind n         note
bind o         overlay

bind e         endSlide
bind Tab       overview
bind BackSpace histBack
